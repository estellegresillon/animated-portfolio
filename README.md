# Magnetic 3D Portfolio

A grid layout with a magnetic 3D hover effect and a content preview animation.

## Installation

Install dependencies:

```
npm install
```

Compile the code for development and start a local server:

```
npm start
```

Create the build:

```
npm run build
```
